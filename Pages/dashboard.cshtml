<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h2 class="text-center">Dashboard</h2>
        <div class="row mb-3">
            <div class="col">
                <h4>Tasks Overview</h4>
                <ul>
                    <li>Total Tasks: <span id="totalTasks">0</span></li>
                    <li>Completed Tasks: <span id="completedTasks">0</span></li>
                    <li>Overdue Tasks: <span id="overdueTasks">0</span></li>
                </ul>
            </div>
            <div class="col">
                <h4>Filter and Sort</h4>
                <div class="form-group">
                    <label for="filterCategory">Category</label>
                    <input type="text" class="form-control" id="filterCategory">
                </div>
                <div class="form-group">
                    <label for="filterPriority">Priority</label>
                    <select class="form-control" id="filterPriority">
                        <option value="">All</option>
                        <option value="Low">Low</option>
                        <option value="Medium">Medium</option>
                        <option value="High">High</option>
                    </select>
                </div>
                <button class="btn btn-primary" onclick="filterTasks()">Filter</button>
                <button class="btn btn-secondary" onclick="sortTasks()">Sort by Due Date</button>
            </div>
        </div>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Due Date</th>
                    <th>Priority</th>
                    <th>Category</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody id="taskTableBody">
                <!-- Tasks will be dynamically inserted here -->
            </tbody>
        </table>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        $(document).ready(function () {
            // Load tasks and populate the dashboard
            // Add your AJAX code to load tasks here

            function filterTasks() {
                // Add your filtering logic here
            }

            function sortTasks() {
                // Add your sorting logic here
            }
        });
    </script>
</body>
</html>
